{
  "name": "electron-printer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean",
    "electron-build": "node-gyp rebuild --arch=ia32 --target=11.5.0 --runtime=electron --dist-url=https://atom.io/download/electron"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-gyp": "^8.4.1"
  },
  "dependencies": {
    "nan": "^2.15.0",
    "node-addon-api": "^4.2.0"
  }
}